<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by Flutter profile VM service (localhost attach/perf tooling). -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
