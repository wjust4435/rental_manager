<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by Flutter debug VM service (localhost attach for run/debug/hot-reload). -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
